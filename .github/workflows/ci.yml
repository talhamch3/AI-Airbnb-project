name: MLOps Monitoring Pipeline CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test-pipeline:
    runs-on: ubuntu-latest

    services:
      mlflow_server:
        image: ghcr.io/mlflow/mlflow:v2.11.3
        ports:
          - 5000:5000
        # FIXED: Combined the entrypoint and startup commands directly into the options string
        options: >-
          --entrypoint mlflow
          server
          --host 0.0.0.0
          --port 5000

    steps:
    - name: Checkout Repository Code
      uses: actions/checkout@v4

    - name: Set up Python Environment
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        cache: 'pip'

    - name: Install Project Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Wait for MLflow & Initialize Experiment
      run: |
        # Gives the background container a brief moment to finish booting up
        sleep 5
        python -c "
        import mlflow
        mlflow.set_tracking_uri('http://mlflow_server:5000')
        mlflow.set_experiment('Airbnb Price Prediction')
        "

    - name: Execute Pytest Suite with Live Network Connections
      env:
        MLFLOW_TRACKING_URI: http://mlflow_server:5000
      run: |
        pytest -s -v